<resources>
    <!-- Application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- ToolBar背景色,主色 -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- 状态栏背景色,深一级的主色 -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- 控件主色 -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <!-- ToolBar的默认样式 -->
        <item name="toolbarStyle">@style/ToolBar</item>
        <item name="toolbarNavigationButtonStyle">@style/Toolbar_NavigationButton</item>
        <item name="actionButtonStyle">@style/ToolBar_ActionButton</item>
        <item name="actionOverflowButtonStyle">@style/ToolBar_ActionButtonOverflow</item>
        <item name="actionOverflowMenuStyle">@style/ToolBar_Popup</item>
        <item name="android:dropDownListViewStyle">@style/MyDropDownListViewStyle</item>

        <item name="actionBarDivider">@drawable/shape_bg_divider_dark</item>
    </style>

    <!-- ======================= Activity ======================= -->
    <!-- 常用Act -->
    <style name="AppTheme.DefActTheme">
    </style>
    <!-- MainAct -->
    <style name="AppTheme.MainActTheme">
    </style>
    <!-- LauncherStyle -->
    <style name="AppTheme.LauncherStyle">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/LauncherAnimation</item>
    </style>

    <style name="LauncherAnimation" parent="@android:style/Animation.Activity">
        <item name="android:windowEnterAnimation">@anim/anim_no</item>
        <item name="android:windowExitAnimation">@anim/system_close_exit</item>
        <item name="android:taskToBackEnterAnimation">@null</item>
        <item name="android:taskToBackExitAnimation">@null</item>
    </style>
    <!-- Search -->
    <style name="AppTheme.SearchTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowEnterAnimation">@anim/system_open_enter</item>
        <item name="android:windowExitAnimation">@anim/alpha_no</item>
    </style>
    <!-- TransStyle -->
    <style name="AppTheme.TransStyle">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/TransStyleAnimation</item>
    </style>

    <style name="TransStyleAnimation" parent="@android:style/Animation.Activity">
        <item name="android:windowEnterAnimation">@anim/alpha_in</item>
        <item name="android:windowExitAnimation">@anim/alpha_out</item>
        <item name="android:taskToBackEnterAnimation">@null</item>
        <item name="android:taskToBackExitAnimation">@null</item>
    </style>
    <!-- ======================= dialog ======================= -->
    <!-- dialog style -->
    <style name="DefDialogStyle" parent="Theme.AppCompat.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!-- 底部进入的Dialog -->
    <style name="BtmDialogStyle" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/AnimationBtm</item>
    </style>

    <style name="AnimationBtm" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/slide_out_bottom</item>
    </style>
</resources>